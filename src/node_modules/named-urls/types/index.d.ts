export interface Include {
    <R extends Routes>(path: string, routes: R): R & {
        toString(): string;
    };
}
export interface Reverse {
    (pattern: string, params?: ReverseParams): string;
}
export interface ReverseParams {
    [path: string]: number | string;
}
export interface Routes {
    [path: string]: string | Routes | (() => string);
}
export declare type ReverseForce = Reverse;
export declare const include: Include;
export declare const reverse: Reverse;
export declare const reverseForce: ReverseForce;
