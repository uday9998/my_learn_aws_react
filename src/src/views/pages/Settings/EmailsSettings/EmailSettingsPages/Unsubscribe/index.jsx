import React from 'react';
import './index.scss';

const UnsubscribeEmailSettingsPage = () => {
   return (
      <div>
            you are now in unsubscribe paeg
      </div>
   );
};

UnsubscribeEmailSettingsPage.propTypes = {};

export default UnsubscribeEmailSettingsPage;
