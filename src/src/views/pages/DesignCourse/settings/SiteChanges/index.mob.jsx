import React from 'react';
import './index.mob.scss';
import SiteChangesModule from 'components/modules/designCourse/settings/SiteChanges';

const SiteChanges = () => {
   return (
      <div className='mob-siteChanges'>
         <SiteChangesModule />
      </div>
   );
};

export default SiteChanges;
