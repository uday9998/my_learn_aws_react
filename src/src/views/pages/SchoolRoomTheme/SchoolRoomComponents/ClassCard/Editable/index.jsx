/* eslint-disable camelcase */
// import React, { useEffect } from 'react';
// import highlightSidebar from 'utils/pageBuilder/highlightSidebar';
// import PropTypes from 'prop-types';
// import ColorInput from 'components/elements/form/ColorInput';
import './index.scss';


const ClassCardEditable = () => {
   // const {
   //     slug,
   //    scroll, menuVisible, toggleSidebar,
   //     index, bgColor, changeProp
   // } = props;

   // useEffect(() => {
   //    if (scroll) {
   //       highlightSidebar(slug, toggleSidebar, menuVisible);
   //    }
   // }, [scroll]);

   return null;

   // return (
   //    <div className='classCardEditable' data-slug={ slug }>
   //       <div>
   //          <ColorInput
   //             label='Background Color'
   //             name='bgColor'
   //             value={ bgColor }
   //             onChange={ (key, value) => changeProp(value, 'bgColor', 'component', index) }
   //             isPageBuilder={ true }
   //          />
   //       </div>
   //    </div>

   // );
};

ClassCardEditable.defaultProps = {
};

ClassCardEditable.propTypes = {
   // scroll: PropTypes.any,
   // menuVisible: PropTypes.bool,
   // toggleSidebar: PropTypes.func,
   // changeProp: PropTypes.func,
   // slug: PropTypes.string,
   // index: PropTypes.number,
   // bgColor: PropTypes.string,
};

export default ClassCardEditable;
