import React from 'react';
import './index.scss';

const DripContent = (
) => {
   return (
      <div className='dripModal' style={ { maxWidth: '100%' } }>
         <div className='congratulationsRectangle' />
      </div>
   );
};

DripContent.propTypes = {

};

export default DripContent;
