import React from 'react';
import PropTypes from 'prop-types';
import AdminContainer from 'views/layout/AdminContainer';

const UpsellCheckout = () => {
   return (
      <AdminContainer>
    asd
      </AdminContainer>
   );
};

UpsellCheckout.propTypes = {

};

export default UpsellCheckout;
